import test from "@/playwright/fixtures/next-fixture"

test("Date Picker - Regression", async ({ page }) => {
  await page.goto("/")
})

test("Date Range Picker - Regression", async ({ page }) => {
  await page.goto("/")
})

test("Date Picker With Presets - Regression", async ({ page }) => {
  await page.goto("/")
})

test("Date Picker With Dropdown - Regression", async ({ page }) => {
  await page.goto("/")
})

test("Date Picker With Disabled Range - Regression", async ({ page }) => {
  await page.goto("/")
})
